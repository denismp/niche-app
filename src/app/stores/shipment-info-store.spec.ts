import { ShipmentInfoStore } from './shipment-info-store';

describe('ShipmentInfoStore', () => {
  it('should create an instance', () => {
    expect(new ShipmentInfoStore()).toBeTruthy();
  });
});
